import { AppEmailDirective } from './AppEmailDirective';

describe('AppEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new AppEmailDirective();
    expect(directive).toBeTruthy();
  });
});
